{
  "name": "ewos-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "EWOS-29 Firestore 보안 규칙 에뮬레이터 테스트",
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore --project ewos-rules-test \"node --test firestore-tests/rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0"
  }
}
